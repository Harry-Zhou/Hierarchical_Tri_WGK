你是一个精通python编程和图论算法设计与实现的高级程序员，严格遵守coding的基本原则：
   - think before coding: 别假设，别隐藏；不确定就问，别瞎猜；有歧义时要多列出几个解释，别自己拍板决定；如有更简单的方法，大胆说出来；搞不清楚的时候，就说明哪里搞不清楚；
   - simplicity first：不要过度设计，不要过度实现，不要过度优化；不要加没有必要且没有要求的功能；不要给只用一次的代码抽象；不要加没有要求的灵活配置或可配置性；不要为不可能发生的场景写错误处理；
   - surgical changes：只改应该改动的部分，严格遵守用户要求，只优化应该优化的部分；
   - Goal-drived executor：按照我方案中所列的目标、技术和方法，循环验证知道代码完成度达到100%（production-ready）
   - 高内聚、低耦合、扁平化的设计原则
   - 修改错误或者警告时，要深入分析错误/警告的原因，并根据原因针对性修复错误/警告；不要为了减少错误/警告的数量，而投机取巧；更不要试图通过隐藏、压制、屏蔽错误/警告来完成修复，比如不要使用类似#![allow(clippy::useless_conversion)]来压制错误或者警告。
   - 当前nexusmind-graphrag已经实现的功能，直接调用相关接口即可，不要重复造轮子
   - 单元测试/集成测试要测试意图，而不是测试行为
   - 对于复杂、长时程任务，要耐心分析，按照规划、执行、验证的循环依次执行。规划时采用分层规划策略，先搭建框架，然后逐层填充细节，直到任务全部完成；采用subagent-driven的策略具体执行任务

1. 请根据上述要求，按照下述step-1、step-2、step-3依次实现这几步描述的算法，将代码保存在项目根目录下cyclic_schema/hierarchical_triangulated_wl.py中。将cyclic_schema.ipynb中用于构建cyclic_schematic_graph，以及示例1中构建多层cyclic schematic graph的代码打包成一个python文件（cyclic_schema.py），hierarchical_triangulated_wl.py中代码用到相关功能构，直接调用cyclic_schema.py中的函数即可。以下是step-1、step-、step-3的算法描述（当前cyclic_schema.py和hierarchical_triangualated_wl.py中已经提供了一个实现，deep audit这两个py文件中的代码，继续根据下述step-1/step-2/step-3描述的算法，增强、补充、修改、完善、优化、升级相关代码）：
   - step-1. cyclic_schema.ipynb中核心算法函数部分，实现了构造cyclic schematic graph的功能。根据cyclic_schematic_graph的输出，构建输入图中节点与所输出的cyclic schematic graph中节点之间的对应关系。其中cyclic schematic graph中节点与输入图中的节点的对应关系如下：
       - 对于cyclic schematic graph中类型为cycle basis的节点， 其对应的输入图中的节点就是该cycle basis中包含的所有输入图中的节点构成的元组；
       - 对于cyclic schematic graph中类型为original_non_cycle的节点，其对应的输入图中的节点就是该original_non_cycle结点编号对应的输入图中的节点（统一以元组格式存储）
       - 对于cyclic schematic graph中类型为interface的节点，其对应的输入图中的节点就是该interface结点编号对应的输入图中的节点（统一以元组格式存储）
       - 对于输入图中的每个结点，该结点对应的cyclic schematic graph中的节点为：（1）如果该结点同时出现在多个cycle basis中，则该结点对应的cyclic schematic graph中的节点为所有包含该结点的cycle basis所对应的cyclic schematic graph中节点构成的元组；（2）如果该节点以original_non_cycle类型出现在cyclic schematic graph中，则其对应cyclic schematic graph中类型为original_non_cycle的具有相同编号的节点（以元组类型存储）; (3) 如果该节点以interface类型出现在cyclic schematic graph中，则其对应cyclic schematic graph中类型为interface的具有相同编号的节点（以元组类型存储）;
   - step-2. 建立了输入图中节点与cyclic schematic graph中节点之间的对应关系以后，相当于将输入图和所生成的cyclic schematic graph统一融合到一个双层图中。假设$G_i=(V_i, E_i), i=1,2$表示一对输入图，$l_{G_i}(v)$表示输入图中节点$v\in V_i$的标签。利用cyclic_schema.ipynb中的cyclic_schematic_graph方法, 可以获得一对cyclic schematic graph，即$CSG[G_i]=(V_{CSG[G_i]}, E_{CSG[G_i]})$。令$SORT(.)$表示接受一个列表（其中元素是可比较的）作为输入，然后将输入列表中元素升序排列后的新列表，比如$SORT([3,2,1])$返回一个升序排好序的列表$[1,2,3]$。下面先建立在一对双层图$(G_i, CSG[G_i])$中一次迭代过程中的消息传递机制（分为从输入图到cyclic schamtic graph的前向消息传递和从cyclic schematic graph到输入图的后向消息传递）：
      - 前向计算最终标签元组：输入图中每个结点都有一个标签（部分数据集上边也有标签）。根据$CSG[G_i]$中每个结点对应的输入图中结点构成的元组，可以为$CSG[G_i]$中每个结点构造一个初始标签元组（该初始标签是一个元组，由该节点对应的输入图中结点元组的各个元素对应的输入图结点标签构成）。对于cyclic schematic graph中类型为cycle basis的节点，其初始标签元组是一个由输入图中多个结点标签构成的元组，还需要对该标签进行标准化处理（因为圈具有对称性，需要按照统一的标准设置cycle basis节点的标签），标准化处理的策略为：在每次迭代过程中，在每个cycle basis类型的节点的初始标签元组中，找到具有最小标签值的第一个元素，从该元素开始，向左可以形成一个左标签元组（cycle具有对称性，沿着cycle向左依次走一圈可以得到原来cycle的一个新的表示，将其中对应节点的标签充足成一个标签元组，即左标签元组），同样按照类似的方式向右可以形成一个右标签元组，选择左、右标签元组中字典序最小的哪个标签元组作为本次迭代过程中该cycle basis节点的最终标签元组。对于original_non_cycle和interface类型的几点，其初始标签元组只有一个元素，所以直接将初始标签元素作为最终的标签元组。这样$CSG[G_i]$中每个结点对应一个最终标签元组。
      - 前向消息传递：利用$CSG[G_i]$中所有节点的最终标签元组，在其上进行消息传递，并获得每个结点的新标签。不妨假设$l_{CSG[G_i]}(v)$表示$v\in V_{CSG[G_i]}$的最终标签元组。在$CSG[G_i]$上一次消息传递过程为：（1）聚集操作。每个节点$v\in V_{CSG[G_i]}$，其聚集到的标签元组根据$v$的邻居结点的结构（邻居结点之间是否在$CSG[G_i]$中相邻）组织在一个元组中，即$AGG_{CSG[G_i]}(v)$是一个聚集标签列表，其第一个元素是元组$(l_{CSG[G_i]}(v), )$（注意：$l_{CSG[G_i]}(v)$本身也是一个元组，第一个位置上的元素是固定的）, 后面的元素由$v$在$CSG[G_i]$中的邻居结点集合$N_{CSG[G_i]}(V)$的结构决定（令$lt_i=[]$且$CSG[G_i](N_{CSG[G_i]}(v))$表示$CSG[G_i]$中由$N_{CSG[G_i]}(v)$中节点诱导出的子图。$CSG[G_i](N_{CSG[G_i]}(v))$可能是非连通图：对于$u\in N_{CSG[G_i]}(v)$且$u$在$CSG[G_i](N_{CSG[G_i]}(v))$中是孤立点，则将$l_{CSG[G_i]}(u)$加入到$lt_i$中；对于$CSG[G_i](N_{CSG[G_i]}(v))$中的一个连通分支中的节点$\{u_1, \cdots, u_s\}$，则将$SORT([l_{CSG[G_i]}(u_1), \cdots, l_{CSG[G_i]}(u_s)])$的返回值（列表）加入到$lt_i$中；）。将$SORT(lt_i)$中的元素依次加入到$AGG_{CSG[G_i]}(v)$中；（2）更新操作。$CSG[G_i]$中每个结点$v\in V_{CSG[G_i]}$都得到$AGG_{CSG[G_i]}(v)$后，更新$v\in V_{CSG[G_i]}$的标签（更新策略：计算两个输入图$G_1， G_2$中值最大的标签，即$l_{max}=\max\left(\max_{v1\in V_1} l_{G_1}(v1), \max_{v2\in V_2} l_{G_2}(v2)\right)$；$CSG[G_1]$和$CSG[G_2]$中所有节点的新标签从$l_{max}+1$开始算；将$CSG[G_1]$和$CSG[G_2]$中所有节点的聚集标签元组（$AGG_{CSG[G_1]}(v)$和$AGG_{CSG[G_2]}(v)$混合在一起排序）的列表升序排列，并且要记录排序后的列表中每个位置对应的是哪个$CSG[G_i]$中哪个节点的编号；最终，按照排好序的列表中每个位置对应的$CSG[G_i]$中节点的编号，依次更新对应$CSG[G_i]$中节点标签为$l_{max}+1, l_{max}+2, \cdots$）;
      - 反向消息传递：在获得$CSG[G_i]$中每个结点的新标签后，下面更新输入图$G_i$中节点的标签。$V_{G_i}$中每个结点$v\in V_{G_i}$也对应一个初始标签列表$[l_{G_i}(v), ]$。每个结点$v\in V_{G_i}$均对应一个由$V_{CSG[G_i]}$中节点构成的元组，从而$V_{G_i}$中每个结点$v\in V_{G_i}$都能获得一个标签元组$SORT([L_{CSG[G_i]}(u): v \text{ belongs to } u \in V_{CSG[G_i]} \text{ corresponding to the cycle basis, or } v \text{ belongs to } u \in V_{CSG[G_i]} \text{ corresponding to the original\_non\_cycle type or interface type nodes}])$，将这个标签元组中的元素按顺序依次加入到$v\in V_{G_i}$初始标签列表$[l_{G_i}(v), ]$中，从而形成$v\in V_{G_i}$的最终标签元组（列表转换成标签）。按照“前向消息传递”中的类似方法，在$CSG[G_1], CSG[G_2]$上执行一次消息传递过程（首先聚集操作，第二更新操作），这时将$CSG[G_1], CSG[G_2]$作为了输入图，使用$V_{G_1}, V_{G_2}$的最终标签元组和“前向消息传递”中类似的过程，可以分别得到$V_{G_1}, V_{G_2}$中所有节点的新标签（注意：这时起始新标签从$CSG[G_1], CSG[G_2]$中所有节点的当前最大标签值+1开始计算，具体过程类似前向消息传递过程）。
   - step-3. 在实际中，根据输入图$G_i, i=1,2$的规模和复杂度，需要建立多层cyclic schematic graph（参考cyclic_schema.ipynb中示例1的“Iteratively refine: use the cyclic schematic graph as input for next iteration”过程）。这时由输入图$G_i, i=1,2$和多层cyclic schematic graph构成了一个多层图$(G_i, CSG^1[G_i], \cdots, CSG^{k}[G_i])$（其中$K$代表层数，默认$K=1$，可以测试$K=2,3$是否能正常工作），并且可以按照step-1中的方法建立相邻两层图中节点之间的对应关系。按照$G_i\rightarrow CSG^1[G_i]\rightarrow\cdots\rightarrow CSG^{K}[G_i]$的顺序，使用step-2中一次迭代过程中的前向计算最终标签元组、前向消息传递机制依次更新前一层图中结点标签。当到达第$K$层后，按照$CSG^K[G_i]\rightarrow\cdots\rightarrow CSG^1[G_i], G_i$的顺序，使用step-2中一次迭代过程中的反向消息传递依次更新后一层（相对于刚才的前进一层，这里相当于后退一层）图中的节点标签。这才完成了多层图$(G_i, CSG^1[G_i], \cdots, CSG^{k}[G_i])$上一次迭代过程中的消息传递。上述消息传递执行$I$次迭代，依次记录输入图$G_1, G_2$中初始的节点标签，以及每次迭代中（一次迭代就是在多层图$(G_i, CSG^1[G_i], \cdots, CSG^{k}[G_i])$上执行一次完整的消息传递，从最底层到最顶层；然后从最顶层返回到最底层，从而更新输入一对图中节点的标签）更新的标签，将每个输入图的所有标签保存在一个ndarray（$|V_1|\times (I+1)$和$|V_2|\times (I+1)$）中，并返回一对ndarray。
   - K=0时，使用hierarchical_cycle_complex_wl_tools/node_wl_via_triangulated_neighbors.py中node_wl_test_triangulated_neighbors方法执行基于triangulated neighbors的WL消息传递
   - 按照add_edge_label_support.md中的方案和hierarchical_triangulated_wl.py中处理带节点标签的输入图的分层消息传递机制，在cyclic_schema/hierarchical_triangulated_wl.py中增加新的函数，用于处理同时带有结点标签和边标签的输入图上的分层消息传递。另外，在hierarchical_triangulated_wl.py中增加一个统一的函数向外部用户提供统一接口（参考hierarchical_cycle_complex_wl_tools/topo_aware_wl_test.py中的代码），根据输入图的g_info字典中保存的关于图数据信息的类型（是否带有边标签，如果不带，则只使用带节点标签的分层消息传递；如果带有边标签，则意味着必然带有结点标签，此时调用能同时处理结点标签和变标签输入图的分层消息传递函数）

2. 同一个图，经过isomorphic-relabeled以后，虽然结点编号不同，但是结点标签不会改变；在每次迭代中都使用统一固定的cycle labeling规则（在每次迭代过程中，在每个cycle basis类型的节点的初始标签元组中，找到具有最小标签值的第一个元素，从该元素开始，向左可以形成一个左标签元组（cycle具有对称性，沿着cycle向左依次走一圈可以得到原来cycle的一个新的表示，将其中对应节点的标签充足成一个标签元组，即左标签元组），同样按照类似的方式向右可以形成一个右标签元组，选择左、右标签元组中字典序最小的哪个标签元组作为本次迭代过程中该cycle basis节点的最终标签元组）。进入下一迭代过程，cycle basis的labeling也会相应改变。

3. nx.minimum_cycle_basis返回的minimum cycle basis是由节点编号构成的元组，但是上述在消息传递过程中，使用的是每个minimum cycle basis的标签（由cycle basis中节点的标签按照labeling 规则构成）

4. 两个输入图的节点标签以ndarray格式作为函数的输入参数（vlabel_np1, vlabel_np2）

5. Integrate hierarchical_triangular_wl_unified into the TopoWassersteinGraphKernel (our_experiments/topo_wasserstein_graph_kernel.py中定义) pipeline so edge-label datasets (PROTEINS, ENZYMES, NCI1, etc.) are properly handled；整合完成后，还要保证在our_experiments/main_topowgk.py中能够正常调用，并在各个数据集上编译成功。
6. Pass g_info through from dataset processing → kernel → WL test。Consider；
7. adding full matrix test comparing edge-labeled vs non-edge-labeled outputs
